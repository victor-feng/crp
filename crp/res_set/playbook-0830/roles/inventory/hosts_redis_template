[redis_master]
{redis_master_ip}

[redis_slave]
{redis_slave_ip}

[redis_servers:children]
redis_master
redis_slave

[redis_master:vars]
role=MASTER
priorityM=100
slaveofIP={redis_slave_ip}

[redis_slave:vars]
role=BACKUP
priorityM=99
slaveofIP={redis_master_ip}

[redis_servers:vars]
routerID={routerID}
network_eth={network_eth}
redis_vip={redis_vip_ip}
redis_port={redis_port}
redis_version={redis_version}
redis_dump_dir=/home/redis/